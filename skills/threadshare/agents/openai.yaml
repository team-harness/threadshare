interface:
  display_name: "Threadshare"
  short_description: "Preflight, share, and read agent sessions"
  default_prompt: "Use $threadshare to safely preflight and share this agent session, then return the verified viewer URL."
